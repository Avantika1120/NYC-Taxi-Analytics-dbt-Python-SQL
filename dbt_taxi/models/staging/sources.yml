version: 2

sources:
  - name: raw
    schema: raw
    description: Raw NYC TLC data loaded by the Python ingestion layer.
    tables:
      - name: yellow_tripdata
        description: Raw Yellow Taxi trip records.
      - name: taxi_zones
        description: Official NYC TLC taxi zone lookup.
